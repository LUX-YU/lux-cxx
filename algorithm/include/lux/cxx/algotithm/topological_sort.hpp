#pragma once
#include <vector>
#include <queue>

namespace lux::cxx::algorithm
{
	template<typename T>
	class TopologicalSort
	{
	public:
		TopologicalSort(std::vector<T>& elements, );


	private:

	};
}
